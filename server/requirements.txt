# --- Core server (Flask + CORS + Socket.IO over eventlet) ---
Flask==3.0.3
flask-cors>=4.0
Flask-SocketIO>=5.3
eventlet==0.36.1
python-dotenv>=1.0
gunicorn==21.2.0   # optional, only for production serving

# --- Data stores (only if you use them) ---
pymongo>=4.6      # optional: MongoDB
firebase-admin>=6.5  # optional: Firebase Admin SDK

# --- NLP / ML ---
transformers>=4.41
sentencepiece>=0.1.99   # needed for T5 summarizer
PyPDF2
# --- File parsing for uploads ---
pdfplumber>=0.11
python-docx>=1.1
